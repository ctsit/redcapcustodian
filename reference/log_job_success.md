# Log a successful job run

Log a successful job run

## Usage

``` r
log_job_success(summary)
```

## Arguments

- summary, :

  the job summary

## Examples

``` r
if (FALSE) { # \dontrun{
 log_job_success(
   summary = "Job succeeded"
 )
} # }
```
