# Log a job debug entry

Log a job debug entry

## Usage

``` r
log_job_debug(summary)
```

## Arguments

- summary, :

  the job summary

## Examples

``` r
if (FALSE) { # \dontrun{
 log_job_debug(
   summary = "Job debug step"
 )
} # }
```
