# Check if "CI" environment variable is set to TRUE

Check if "CI" environment variable is set to TRUE

## Usage

``` r
is_on_ci()
```

## Value

TRUE/FALSE

## Examples

``` r
if (FALSE) { # \dontrun{
  is_on_ci()
} # }
```
