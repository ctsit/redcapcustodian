# Fetches the current time in system time zone

Fetches the current time in system time zone

## Usage

``` r
get_current_time()
```

## Value

A duration object representing the current time

## Examples

``` r
if (FALSE) { # \dontrun{
get_current_time()
} # }
```
