# Log a failed job run

Log a failed job run

## Usage

``` r
log_job_failure(summary)
```

## Arguments

- summary, :

  the job summary

## Examples

``` r
if (FALSE) { # \dontrun{
 log_job_failure(
   summary = "Job failed"
 )
} # }
```
